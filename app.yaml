steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/all-about-kam/webpage', '.']
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/all-about-kam/webpage']
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: ['run', 'deploy', 'kamservice', '--image', 'gcr.io/all-about-kam/webpage', '--region', 'REGION', '--platform', 'managed']
images:
  - gcr.io/all-about-kam/webpage